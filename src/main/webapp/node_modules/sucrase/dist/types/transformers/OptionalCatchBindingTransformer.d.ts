import type NameManager from "sucrase/dist/types/NameManager";
import type TokenProcessor from "sucrase/dist/types/TokenProcessor";
import Transformer from "sucrase/dist/types/transformers/Transformer";
export default class OptionalCatchBindingTransformer extends Transformer {
    readonly tokens: TokenProcessor;
    readonly nameManager: NameManager;
    constructor(tokens: TokenProcessor, nameManager: NameManager);
    process(): boolean;
}

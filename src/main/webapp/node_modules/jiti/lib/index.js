function onError(err) {
  throw err; /* ↓ Check stack trace ↓ */
}

module.exports = function jiti(filename, opts) {
  const jiti = require("jiti");

  opts = { onError, ...opts };

  if (!opts.transform) {
    opts.transform = require("jiti/dist/babel");
  }

  return jiti(filename, opts);
};

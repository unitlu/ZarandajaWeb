import { map } from 'yaml/browser/dist/schema/common/map.js';
import { nullTag } from 'yaml/browser/dist/schema/common/null.js';
import { seq } from 'yaml/browser/dist/schema/common/seq.js';
import { string } from 'yaml/browser/dist/schema/common/string.js';
import { boolTag } from 'yaml/browser/dist/schema/core/bool.js';
import { float, floatExp, floatNaN } from 'yaml/browser/dist/schema/core/float.js';
import { int, intHex, intOct } from 'yaml/browser/dist/schema/core/int.js';
import { schema } from 'yaml/browser/dist/schema/core/schema.js';
import { schema as schema$1 } from 'yaml/browser/dist/schema/json/schema.js';
import { binary } from 'yaml/browser/dist/schema/yaml-1.1/binary.js';
import { merge } from 'yaml/browser/dist/schema/yaml-1.1/merge.js';
import { omap } from 'yaml/browser/dist/schema/yaml-1.1/omap.js';
import { pairs } from 'yaml/browser/dist/schema/yaml-1.1/pairs.js';
import { schema as schema$2 } from 'yaml/browser/dist/schema/yaml-1.1/schema.js';
import { set } from 'yaml/browser/dist/schema/yaml-1.1/set.js';
import { timestamp, floatTime, intTime } from 'yaml/browser/dist/schema/yaml-1.1/timestamp.js';

const schemas = new Map([
    ['core', schema],
    ['failsafe', [map, seq, string]],
    ['json', schema$1],
    ['yaml11', schema$2],
    ['yaml-1.1', schema$2]
]);
const tagsByName = {
    binary,
    bool: boolTag,
    float,
    floatExp,
    floatNaN,
    floatTime,
    int,
    intHex,
    intOct,
    intTime,
    map,
    merge,
    null: nullTag,
    omap,
    pairs,
    seq,
    set,
    timestamp
};
const coreKnownTags = {
    'tag:yaml.org,2002:binary': binary,
    'tag:yaml.org,2002:merge': merge,
    'tag:yaml.org,2002:omap': omap,
    'tag:yaml.org,2002:pairs': pairs,
    'tag:yaml.org,2002:set': set,
    'tag:yaml.org,2002:timestamp': timestamp
};
function getTags(customTags, schemaName, addMergeTag) {
    const schemaTags = schemas.get(schemaName);
    if (schemaTags && !customTags) {
        return addMergeTag && !schemaTags.includes(merge)
            ? schemaTags.concat(merge)
            : schemaTags.slice();
    }
    let tags = schemaTags;
    if (!tags) {
        if (Array.isArray(customTags))
            tags = [];
        else {
            const keys = Array.from(schemas.keys())
                .filter(key => key !== 'yaml11')
                .map(key => JSON.stringify(key))
                .join(', ');
            throw new Error(`Unknown schema "${schemaName}"; use one of ${keys} or define customTags array`);
        }
    }
    if (Array.isArray(customTags)) {
        for (const tag of customTags)
            tags = tags.concat(tag);
    }
    else if (typeof customTags === 'function') {
        tags = customTags(tags.slice());
    }
    if (addMergeTag)
        tags = tags.concat(merge);
    return tags.reduce((tags, tag) => {
        const tagObj = typeof tag === 'string' ? tagsByName[tag] : tag;
        if (!tagObj) {
            const tagName = JSON.stringify(tag);
            const keys = Object.keys(tagsByName)
                .map(key => JSON.stringify(key))
                .join(', ');
            throw new Error(`Unknown custom tag ${tagName}; use one of ${keys}`);
        }
        if (!tags.includes(tagObj))
            tags.push(tagObj);
        return tags;
    }, []);
}

export { coreKnownTags, getTags };

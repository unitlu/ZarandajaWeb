import { Task } from 'fast-glob/out/managers/tasks';
import { Entry, EntryItem, ReaderOptions } from 'fast-glob/out/types';
import ReaderAsync from 'fast-glob/out/readers/async';
import Provider from 'fast-glob/out/providers/provider';
export default class ProviderAsync extends Provider<Promise<EntryItem[]>> {
    protected _reader: ReaderAsync;
    read(task: Task): Promise<EntryItem[]>;
    api(root: string, task: Task, options: ReaderOptions): Promise<Entry[]>;
}
